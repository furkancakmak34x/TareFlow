<Window x:Class="TareFlow.Agent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TareFlow Kantar Köprüsü" Height="540" Width="460"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Border Width="44" Height="44" CornerRadius="12" Background="{DynamicResource PrimaryBrush}">
                <TextBlock Text="&#xE2C7;" FontFamily="{StaticResource GoogleMaterialSymbols}" FontSize="26"
                           Foreground="{DynamicResource OnPrimaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Kantar Köprüsü" FontSize="20" FontFamily="{StaticResource GoogleSansBold}"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="COM portunu merkeze TCP ile yayınlar"
                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
            </StackPanel>
        </StackPanel>

        <!-- Ayarlar kartı -->
        <Border Grid.Row="1" Margin="0,18,0,0" Padding="18" CornerRadius="14"
                Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="COM Port" Margin="0,0,12,10" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                <ComboBox x:Name="cmbPort" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" IsEditable="True"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Baud Rate" Margin="0,0,12,10" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                <ComboBox x:Name="cmbBaud" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
                    <ComboBoxItem>2400</ComboBoxItem>
                    <ComboBoxItem>4800</ComboBoxItem>
                    <ComboBoxItem IsSelected="True">9600</ComboBoxItem>
                    <ComboBoxItem>19200</ComboBoxItem>
                    <ComboBoxItem>38400</ComboBoxItem>
                    <ComboBoxItem>57600</ComboBoxItem>
                    <ComboBoxItem>115200</ComboBoxItem>
                </ComboBox>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Dinleme Portu" Margin="0,0,12,0" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="txtListenPort" Grid.Row="2" Grid.Column="1"/>
            </Grid>
        </Border>

        <!-- Kontrol + durum -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnStart" Grid.Column="0" Content="Başlat" Click="btnStart_Click"
                    Width="110" Style="{StaticResource PrimaryButton}"/>
            <Button x:Name="btnStop" Grid.Column="1" Content="Durdur" Click="btnStop_Click"
                    Width="110" Margin="10,0,0,0" Style="{StaticResource DangerButton}" IsEnabled="False"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Ağırlık " Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblWeight" Text="—" FontFamily="{StaticResource GoogleSansBold}"
                           Foreground="{DynamicResource SecondaryBrush}" FontSize="18" VerticalAlignment="Center"/>
                <TextBlock Text="  ·  Merkez " Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblClients" Text="0" FontFamily="{StaticResource GoogleSansBold}" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Log -->
        <Border Grid.Row="3" Margin="0,14,0,0" CornerRadius="12"
                Background="{DynamicResource SurfaceVariantBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <ScrollViewer x:Name="logScroll" VerticalScrollBarVisibility="Auto" Padding="12">
                <TextBlock x:Name="txtLog" FontFamily="Consolas" FontSize="12"
                           Foreground="{DynamicResource SuccessBrush}" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
